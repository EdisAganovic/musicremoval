[project]
name = "demucspleeter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10.0"
dependencies = []

[project.scripts]
demucspleeter = "demucspleeter:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
